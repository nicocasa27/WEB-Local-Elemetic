# Dependencias directas del MES de Elemetic.
# Versiones tomadas del entorno que corre en producción (.venv, Windows).
# Para el conjunto completo con dependencias transitivas ver requirements.lock.txt.
#
# Instalación:  pip install -r requirements.txt
# Python:       3.12 (el sistema corre 3.12/3.13; Django 5.2 exige >= 3.10)

Django==5.2.12

# Driver de PostgreSQL. Django 5.2 prefiere psycopg 3 cuando está disponible.
# El entorno de producción tiene además psycopg2-binary 2.9.9 instalado, que
# quedó sin usarse: no hace falta reinstalarlo.
psycopg[binary]==3.3.3

# Sirve los archivos estáticos desde el propio Django, con hash en el nombre y
# compresión. Hace falta porque el taller trabaja sin internet: hasta ahora
# Bootstrap, los iconos y las gráficas venían de un CDN y sin red la aplicación
# se quedaba sin estilos y sin JavaScript.
whitenoise==6.12.0

# Exportación de reportes y expedientes a Excel.
openpyxl==3.1.5

# Lectura de PDF. Hoy no lo usa nada del sistema en marcha: su único usuario
# era el importador de vigas, que estaba sin ruta y se aparcó en
# attic/importador-de-pdf/. Se deja instalado y con la versión fijada porque
# ese analizador vuelve cuando se monte el inventario, para leer certificados
# de material, y porque el taller no tiene internet: lo que no esté instalado
# el día que haga falta, no se puede instalar.
pypdf==6.9.2
